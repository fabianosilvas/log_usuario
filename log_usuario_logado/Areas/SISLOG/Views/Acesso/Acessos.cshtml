@model IEnumerable<log_usuario_logado.Areas.SISLOG.Models.logtb002_log_acesso>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dh_acesso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.co_sessao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.de_pagina)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.dh_acesso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.co_sessao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.de_pagina)
            </td>
        </tr>
    }

</table>


